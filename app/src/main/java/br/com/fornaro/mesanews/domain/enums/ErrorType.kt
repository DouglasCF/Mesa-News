package br.com.fornaro.mesanews.domain.enums

enum class ErrorType {
    NO_INTERNET,
    INVALID_NAME,
    INVALID_EMAIL,
    INVALID_PASSWORD,
    INVALID_CONFIRM_PASSWORD,
    INVALID_TOKEN,
    GENERIC_ERROR,
}